from setuptools import find_packages, setup

setup(
    name='FileManager',
    packages=find_packages(include=["FileManager"]),
    version='0.0.1',
    description='',
    author='Me',
    license='MIT',
    install_requires=[],
)
